services:

  # FastAPI application
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis

  # Redis cache server
  redis:
    image: redis:7-alpine